policies:
  - name: cis-1-9-iam-password-policy-missing
    resource: aws.account
    description: |
      CIS 1.9 - IAM account password policy does not enforce strong passwords
      (or is not configured at all).
      Severity: HIGH
    filters:
      - type: password-policy
        key: MinimumPasswordLength
        value: absent
